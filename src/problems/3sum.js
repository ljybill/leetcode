/**
 * @param {number[]} nums
 * @return {number[][]}
 */
var threeSum = function(nums) {
    let result = []
  // 从小到大排
  nums.sort((a, b) => a - b);
  let fp = 0
  let sp = 1
  while(sp < nums.length){
      if(nums[fp] + nums[sp]){
          
      }
  }
};

console.log(threeSum([-1, 0, 1, 2, -1, -4]));
